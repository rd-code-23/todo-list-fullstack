version: "3.8"
services:
  client:
    build: ./client
    ports: 
      - '3000:3000'
    volumes: 
      - ./client/src:/app/src
    stdin_open: true
    tty: true
    # depends_on: 
    #   - server